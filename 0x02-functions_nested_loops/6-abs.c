#include "main.h"
/**
 * _abs - A function that computes the absolute value of a numbe
 * @n: the parameter which is always a number
 *
 * Description: the if/else statement returns the number
 * with a positve or negative sign
 * Return: the number with the sign.
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
