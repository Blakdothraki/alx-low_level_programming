#include "main.h"
/**
 *Header file created by blakdothraki
 * A function that computes the absolute value of a number
 */

int _abs(int n)
{
	if ( n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
